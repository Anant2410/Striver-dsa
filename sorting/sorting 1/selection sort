void selectionSort(int arr[], int n)
    {
       int mini;
       for(int i=0;i<n;i++){
           mini = i;
           for(int j=i+1;j<n;j++){
               if(arr[j] < arr[mini]) mini = j;
           }
           swap(arr[mini],arr[i]);
       }
    }
