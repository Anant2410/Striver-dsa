void bubbleSort(int arr[], int n)
    {
        if(n==0) return ;
        for(int i=0;i<n-1;i++){
            if(arr[i] > arr[i+1]){
                int x = arr[i];
                arr[i] = arr[i+1];
                arr[i+1] = x;
            }
        }
        bubbleSort(arr,n-1);
    }
