int findMaxConsecutiveOnes(vector<int>& nums) {
        int cnt = 0;
        int maxi = INT_MIN;
        for(auto it: nums)
        {
            if(it==0) cnt = 0;
            else{
                cnt += 1;
            }
            maxi = max(maxi, cnt);
        }
        return maxi;
    }
